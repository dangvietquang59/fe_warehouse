export type RegisterType = {
    email: string;
    fullname: string;
    username: string;
    password: string;
    confirmPassword: string;
};

export type Create_RegisterTyppe = {
    email: string;
    fullname: string;
    username: string;
    password: string;
};
